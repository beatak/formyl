window.formyl = (function () {
  "use strict";

  var formyl = {};
  formyl.create = function (name, opt) {
    
  };

  var generate_selectors = function (name) {
    return {
      SELECTOR_TEXT: ['.', name, '-select-text'].join(''),
      SELECTOR_WRAP: ['.', name, '-select-text'].join(''),
      SELECTOR_CHECKBOX: ['.', name, '-select-text'].join(''),
      
    };
  };

  var oneform = function (elm) {
    
  };


  return formyl;
})();